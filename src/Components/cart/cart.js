import React, { useContext } from "react";
import Button from "../layout/cartbtn/Button";
const Cart = (props) => {
  return (
    <React.Fragment>
      <Button />
    </React.Fragment>
  );
};
export default Cart;
