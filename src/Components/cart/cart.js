import React, { useContext } from "react";
import Button from "../layout/cartbtn/Button";
const Cart = (props) => {
  return (
    <React.Fragment>
      <nav>
        <Button />
      </nav>
    </React.Fragment>
  );
};
export default Cart;
